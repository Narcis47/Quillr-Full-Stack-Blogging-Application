# 🪶 Quillr

A full-stack blogging platform — register, customize your profile, and create, manage or explore posts written by the community.

Built with Java & Spring Boot on the backend, and a vanilla HTML/CSS/JS frontend generated with AI assistance for rapid backend testing and development.

---

## ✨ Features

- 🔐 User registration and secure login with BCrypt password hashing
- 👤 Automatic profile creation upon registration
- 🎨 Customize profiles with bio, avatar URL, and personal website
- 📝 Complete CRUD operations for blog posts
- ✍️ Rich text editor with bold, italic, underline, headings, lists, links and more
- 🔍 Search posts by title and find users by username
- 📄 Pagination and sorting for posts
- ✅ Input validation with Jakarta Validation
- 📖 Swagger UI for API documentation and testing
- 🌍 CORS configured for local and remote development

---

## 🛠️ Tech Stack

### Backend
| Layer | Technology |
|---|---|
| Language | Java 21 |
| Framework | Spring Boot 3.5 |
| Database | PostgreSQL |
| Database Mapper | Spring Data JDBC |
| Security | Spring Security + BCrypt |
| Validation | Jakarta Validation |
| API Docs | Swagger UI (SpringDoc OpenAPI) |
| Utilities | Lombok |
| Build Tool | Maven |

### Frontend
| Layer | Technology |
|---|---|
| Language | Vanilla HTML / CSS / JavaScript |
| Fonts | Playfair Display + DM Sans (Google Fonts) |
| API Layer | Fetch API (async/await) |
| Generated by | Claude (Anthropic) — AI-assisted for backend testing |

> **Note:** The frontend was fully generated with AI (Claude by Anthropic) to provide a quick, functional interface for testing all backend endpoints during development. It is not intended as a production UI, but covers every endpoint with proper validation, error handling, and session management.

---

## 📁 Project Structure

```
src/main/java/com/narcis/quillr/
├── controller/
│   ├── UserController.java       ← /api/users
│   ├── ProfileController.java    ← /api/profile
│   └── PostController.java       ← /api/posts
├── service/
│   ├── UserService.java
│   ├── ProfileService.java
│   └── PostService.java
├── repository/
│   ├── UserRepository.java
│   ├── ProfileRepository.java
│   └── PostRepository.java
├── model/
│   ├── User.java
│   ├── Profile.java
│   └── Post.java
├── SecurityConfig.java
└── QuillrApplication.java

frontend/
├── config.js          ← API base URL
├── api.js             ← Centralized API layer (all endpoints)
├── shared.css         ← Design system (nav, buttons, cards, toast)
├── style.css          ← Login/Register page styles
├── index.html         ← Login & Register (animated card stack)
├── index.js           ← Auth logic
├── library.html       ← Post feed + search posts + search users
├── post.html          ← Read post, edit/delete (owner only)
├── write.html         ← Create / edit post (rich text editor)
├── profile.html       ← User profile + their posts
└── settings.html      ← Edit bio, avatar, website
```

---

## 🔌 API Endpoints

### Users
| Method | Endpoint | Description | Frontend page |
|---|---|---|---|
| POST | `/api/users/register` | Register a new user | `index.html` |
| POST | `/api/users/login` | Login | `index.html` |
| GET | `/api/users/{id}` | Get user by ID | `profile.html`, `library.html` |
| GET | `/api/users/username/{username}` | Get user by username | `library.html` (People search) |

### Profiles
| Method | Endpoint | Description | Frontend page |
|---|---|---|---|
| GET | `/api/profile/{userId}` | Get profile by user ID | `profile.html`, `settings.html` |
| GET | `/api/profile/username/{username}` | Get profile by username | `profile.html` |
| PUT | `/api/profile/{userId}` | Update profile | `settings.html` |

### Posts
| Method | Endpoint | Description | Frontend page |
|---|---|---|---|
| POST | `/api/posts/create` | Create a new post | `write.html` |
| GET | `/api/posts/` | Get all posts | — |
| GET | `/api/posts/{id}` | Get post by ID | `post.html` |
| GET | `/api/posts/user/{userId}` | Get all posts by user ID | `profile.html` |
| GET | `/api/posts/user/username/{username}` | Get all posts by username | `profile.html` |
| GET | `/api/posts/search?query=` | Search posts by title | `library.html` |
| GET | `/api/posts/paged?page=0&size=10&sortBy=createdAt` | Get posts paginated + sorted | `library.html` |
| GET | `/api/posts/user/{userId}/paged` | Get user posts paginated | — |
| PUT | `/api/posts/{id}` | Update a post | `write.html` (edit mode) |
| DELETE | `/api/posts/{id}` | Delete a post | `post.html` |

---

## 🖥️ Frontend Pages

| Page | Route | Description |
|---|---|---|
| Login / Register | `index.html` | Animated stacked card UI. Auto-login after register. Redirects to library if already logged in. |
| Library | `library.html` | Paginated post feed with sort, post search by title, and people search by username. |
| Post | `post.html` | Full post view with rich HTML rendering. Edit and Delete buttons visible only to the post author. |
| Write | `write.html` | Rich text editor with toolbar (bold, italic, underline, strikethrough, headings, alignment, lists, links, dividers). Also used for editing via `?edit={id}`. |
| Profile | `profile.html` | User profile with bio, avatar, website, and all their posts. Edit profile button visible only to owner. Accepts `?userId=` or `?username=`. |
| Settings | `settings.html` | Edit bio, avatar URL (with live preview), and website. Read-only account info panel. |

---

## 📖 API Documentation

Swagger UI available at:
```
http://localhost:8081/swagger-ui/index.html
```

---

## 🗄️ Database Schema

```sql
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE posts (
    id SERIAL PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    content TEXT NOT NULL,
    user_id INTEGER REFERENCES users(id),
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE profile (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id) UNIQUE,
    bio TEXT,
    avatar_url VARCHAR(255),
    website VARCHAR(255)
);
```

---

## 🚀 Setup & Installation

### Prerequisites
- Java 21
- PostgreSQL
- Maven

### Steps

**1. Clone the repository**
```bash
git clone https://github.com/Narcis47/Quillr-Full-Stack-Blogging-Application.git
cd Quillr-Full-Stack-Blogging-Application
```

**2. Create the PostgreSQL database**
```sql
CREATE DATABASE quillr;
```

**3. Run the schema** (copy SQL from above into DataGrip or psql)

**4. Set environment variables**

IntelliJ → Run/Debug Configurations → Environment Variables:
```
DB_USERNAME=your_postgres_username
DB_PASSWORD=your_postgres_password
```

PowerShell:
```powershell
$env:DB_USERNAME="your_postgres_username"
$env:DB_PASSWORD="your_postgres_password"
```

**5. Run the backend**
```bash
./mvnw spring-boot:run
```

API runs at `http://localhost:8081`
Swagger UI at `http://localhost:8081/swagger-ui/index.html`

**6. Open the frontend**

Open `frontend/index.html` directly in your browser, or serve it with any static server:
```bash
cd frontend
npx serve .
```

The frontend connects to `http://localhost:8081` by default. To change this, edit `config.js`:
```js
const API_URL = 'http://localhost:8081';
```

---

## 📝 Example API Requests

**Register**
```json
POST /api/users/register
{
    "username": "narcis",
    "email": "narcis@example.com",
    "password": "securepassword"
}
```

**Login**
```json
POST /api/users/login
{
    "email": "narcis@example.com",
    "password": "securepassword"
}
```

**Create Post**
```json
POST /api/posts/create
{
    "userId": 1,
    "title": "My First Post on Quillr",
    "content": "<p>Hello <strong>world</strong>!</p>"
}
```

**Search Posts**
```
GET /api/posts/search?query=hello
```

**Get Posts Paginated**
```
GET /api/posts/paged?page=0&size=10&sortBy=createdAt
```

**Find User by Username**
```
GET /api/users/username/narcis
```

**Get Posts by Username**
```
GET /api/posts/user/username/narcis
```

**Get Profile by Username**
```
GET /api/profile/username/narcis
```

**Update Profile**
```json
PUT /api/profile/1
{
  "bio": "Software Developer & Writer",
  "avatarUrl": "https://example.com/avatar.png",
  "website": "https://narcis47.github.io"
}
```

---

## 👤 Author

**Narcis** — [@Narcis47](https://github.com/Narcis47)

---

## 🤖 AI Disclosure

The frontend interface for this project was generated using **Claude** by [Anthropic](https://anthropic.com). It was built to accelerate backend development by providing a complete, working UI that exercises every API endpoint — including auth flows, rich text editing, paginated feeds, profile management, and user/post search — without spending time on manual frontend development.